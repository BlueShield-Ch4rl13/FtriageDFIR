# Parseo de artefactos (Python puro, multiplataforma)
python-evtx>=0.7.4      # event logs .evtx
python-registry>=1.3.1  # hives del registro (amcache, shimcache)
lxml>=4.9               # parseo XML de los eventos

# Prefetch: no requiere paquete. El parseo usa PECmd (Eric Zimmerman EZ Tools)
# si está en el PATH o en --ez-tools; el formato comprimido de Windows 10
# necesita descompresión nativa de Windows, por eso no se reimplementa aquí.
